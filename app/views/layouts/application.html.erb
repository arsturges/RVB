<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
       "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <title>DALM: <%= controller.action_name %></title>
  <%= javascript_include_tag 'jquery','jquery-ui','jrails',  'jquery.tablednd_0_5', 'jquery.form', 'jquery.dragsort-0.3', 'jquery.autocomplete', 'cycle', 'admin',  'application', :cache => true %>
  <%= stylesheet_link_tag 'reset', 'cockpit', 'admin' %>
  <%= stylesheet_link_tag "ui-lightness/jquery-ui-1.7.2.custom" %>

</head>
<body>
    <div id="identity">
      <h2><%= link_to 'DOE Appliance Legislation', root_path %></h2>

<% if logged_in? %>
        <h3>You are logged in as <%=h current_user.login %>.</h3>
        <h3><%= link_to 'Log out', logout_path %></h3>
      <% else %>
        <h3><%= link_to 'Log in', login_path %></h3>
  <% end %>
    </div>
    
    <div id="wrapper">
      <div id="container">
        <div class="banner section">
          <%= render :partial => 'layouts/navigation' %>
        </div>
        <div class="header section">
          <h1><%= yield :title %></h1>
          <%- if (header = yield :header) && !@show_header == true %>
            <%= header %>
          <% end %>
        </div>
        <div class="content section">
          <%= reports_for_page %>
          <p style="color: green"><%= flash[:notice] %></p> <!-- is this line repeating the one above? -->
          <%= yield %>
        </div>
      </div>
    </div>
</body>
</html>
